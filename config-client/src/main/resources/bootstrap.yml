spring:
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config-server
      name: config
      label: master
      profile: dev

  application:
    name: config
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

