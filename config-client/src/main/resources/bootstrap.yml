spring:
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config-server
      name: config
      label: master
      profile: dev

  application:
    name: config
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
